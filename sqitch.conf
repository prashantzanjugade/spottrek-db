[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
[rebase]
	verify = true
[target "spottrek"]
	uri = db:pg://postgres:123@localhost:5432/spottrek
[engine "pg"]
	target = spottrek
